import React from "react";
import ReactDOM from "react-dom/client";
import InvoiceToExcel from "./InvoiceToExcel";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <InvoiceToExcel />
  </React.StrictMode>
);
